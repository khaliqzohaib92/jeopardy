import React, { PropTypes } from 'react';

const Clue = props => {
    return (
        <div>
            
        </div>
    );
};

Clue.propTypes = {
    clue: PropTypes.object.isRequired,
};

export default Clue;